services:
  - type: web
    name: zeiterfassung
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: zeiterfassung-db
          property: connectionString
      - key: ADMIN_PINS
        value: "0000"  # Ändern für Produktion!

databases:
  - name: zeiterfassung-db
    databaseName: zeiterfassung
    user: zeiterfassung
    plan: free  # 1 GB Storage